fastapi>=0.111.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
pydantic[email]>=2.7.0
pydantic-settings>=2.2.1
redis>=5.0.0
rq>=1.16.0
python-docx>=1.1.0
lxml>=5.2.0
Pillow>=10.3.0
Jinja2>=3.1.4
slowapi>=0.1.9
pytest>=8.2.0
httpx>=0.27.0

# Auth + DB (added in v1.2 for user accounts + conversion history)
SQLAlchemy>=2.0.0
psycopg2-binary>=2.9.9
alembic>=1.13.0
passlib[bcrypt]>=1.7.4
bcrypt<4.1  # pin: passlib 1.7.x is incompatible with bcrypt 4.1+ (72-byte error)
python-jose[cryptography]>=3.3.0
authlib>=1.3.0
itsdangerous>=2.2.0

# Object storage (only used when FIGURE_STORAGE=s3 — S3 / Cloudflare R2 / MinIO).
# Present in the image so the documented S3 mode works out of the box; the
# default FIGURE_STORAGE=redis path never imports it.
boto3>=1.34.0
